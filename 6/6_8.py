def rotation(list):
    n_colunas = input('Numero de colunas: ')
    n_linhas = input('Numero de linhas: ')

    coluna = [input() for i in range(n_colunas)]
    linha = [input() for i in range(n_linhas)]

    for i in range(list):
        for a in range(list[i]):


